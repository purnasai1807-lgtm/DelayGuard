# Alembic migrations

Alembic migration directory. For the hackathon MVP, startup creates missing tables; production deployments should add versioned revisions with `alembic revision --autogenerate` and run `alembic upgrade head`.
